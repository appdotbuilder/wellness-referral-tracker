import { type DoctorWithOffice } from '../schema';

export async function getPendingReferrals(): Promise<DoctorWithOffice[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all doctor referrals with pending approval status.
  // This will be used by administrators to view and manage the approval queue.
  // Returns referrals with office information joined for better context.
  return Promise.resolve([]);
}